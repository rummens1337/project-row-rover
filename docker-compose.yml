version: '3'
services:
  webapp:
    build: .
    # devices:
    #   - "/dev/i2c-1"
    #   - "/dev/gpiomem"
    #   - "/dev/vchiq"
    ports:
      - "80:80"
      - "8080:8080"
    volumes:
      - "./appdata:/appdata"
      - "./settings.amd64.template.conf:/app/settings.conf"
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: HRvu4CX5
    volumes:
      - "./mysql:/var/lib/mysql"